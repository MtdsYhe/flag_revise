# 基于 FLAG 的双通道 LLM 增强 GNN 欺诈检测框架技术路线文档

## 面向 Amazon Magazine_Subscriptions 数据集的语义-行为协同构图与欺诈检测研究

---

# 1. 研究目标

本研究旨在在 FLAG（Fraud Detection with LLM-enhanced Graph Neural Network）框架基础上，构建一个面向 Amazon Review 场景的双通道 LLM 增强 GNN 欺诈检测系统。

系统重点解决以下问题：

1. 欺诈评论文本具有较强语言伪装性；
2. 用户行为存在协同刷评模式；
3. 原始图结构缺乏用户间潜在协同行为关系；
4. 单一语义建模难以有效识别伪装型欺诈用户。

因此，本研究提出：

- 语义-行为双通道建模
- 协同伪装图（HCCG）构建
- 语义-行为联合采样
- LLM 引导的图结构增强
- 双通道 GNN 表示学习

---

# 2. 整体技术路线

整体技术流程如下：

```text
原始评论数据
→ 数据预处理
→ 用户行为统计
→ 文本语义编码
→ HCCG 图构建
→ 联合邻居采样
→ Qwen2.5 双通道生成
→ Frozen LM 编码
→ GCN/GAT 图传播
→ 双通道注意力融合
→ 欺诈预测
```

整个系统主要由以下部分组成：

1. 数据层
2. 图构建层
3. 双通道生成层
4. 图学习层
5. 多目标优化层

---

# 3. 数据预处理

使用 Amazon Magazine_Subscriptions.jsonl 数据集。

## 3.1 数据字段

主要字段包括：

- reviewText
- overall
- reviewerID
- asin
- unixReviewTime
- helpful

## 3.2 数据清洗

数据预处理包括：

- 评论文本清洗
- HTML 标签与特殊字符去除
- 时间戳标准化
- 用户评论聚合
- 商品评论聚合
- 行为统计特征提取

---

# 4. 用户行为统计建模

对每个用户构建行为统计特征，包括：

| 特征 | 含义 |
|---|---|
| avg_rating | 平均评分 |
| rating_std | 评分标准差 |
| review_count | 评论数量 |
| helpful_ratio | 有用率 |
| avg_interval_days | 平均评论时间间隔 |
| burstiness | 时间突发性 |

## 4.1 行为特征作用

行为特征主要用于刻画：

- 用户评分倾向
- 评论频率
- 时间规律
- 潜在协同行为
- 行为异常模式

## 4.2 时间突发性

时间突发性用于衡量：

> 用户是否在短时间内集中发布评论。

欺诈团伙通常具有：

- 集中刷评
- 时间同步
- 高密度评论行为

因此该特征对欺诈检测具有较强区分能力。

---

# 5. 文本语义编码

采用 Sentence-BERT 或 MiniLM 对评论文本进行编码。

## 5.1 文本编码作用

文本编码主要用于：

1. 用户语义相似度计算
2. 联合采样
3. HCCG 图构建
4. LLM 输入增强

## 5.2 用户级文本表示

将用户历史评论进行聚合，得到用户级文本表示。

随后通过预训练语言模型生成语义嵌入，用于后续图建模。

---

# 6. HCCG 协同伪装图构建

# Heterogeneous Collaborative Camouflage Graph

这是本研究最核心的创新之一。

## 6.1 图结构组成

图节点包括：

- 用户节点（User）
- 商品节点（Product）

图边包括：

- User-Product 交互边
- User-User 协同行为边

---

## 6.2 User-Product 边

表示：

> 用户评论了某商品。

用于保留原始评论交互关系。

---

## 6.3 User-User 协同行为边

用于建模：

- 潜在协同行为
- 伪装关系
- 欺诈团伙结构

由于 Amazon Review 不存在天然社交图，因此需要通过用户相似性主动构建。

---

# 7. 用户协同行为边构建

用户协同行为边通过多维相似度融合构建。

## 7.1 语义相似度

通过文本嵌入计算：

- 评论内容相似性
- 语言风格相似性
- 情绪表达一致性

---

## 7.2 行为相似度

通过行为统计向量计算：

- 评分倾向
- 评论频率
- 时间规律
- 投票行为

欺诈用户通常具有高度相似行为模式。

---

## 7.3 商品重叠度

若两个用户评论了大量相同商品，则说明可能存在：

- 协同行为
- 团伙刷评
- 共同操纵行为

因此商品重叠度是重要欺诈信号。

---

## 7.4 时间一致性

若两个用户：

- 在短时间内
- 对相同商品
- 给出一致评分

则增强二者边权。

该机制用于建模：

> 时间同步刷评行为。

---

## 7.5 联合边权

最终边权由以下部分共同决定：

- 语义相似度
- 行为相似度
- 商品重叠度
- 时间一致性

该机制能够有效建模：

# “协同伪装关系”

---

# 8. Top-K 用户关系构建

为了避免图结构过于稠密，本研究采用：

# Top-K 建边策略

对于每个用户：

1. 计算所有候选用户相似度；
2. 保留最相似的 K 个邻居；
3. 建立 User-User 协同行为边。

## 8.1 Top-K 机制优势

该机制能够：

- 提升局部图质量
- 降低噪声边
- 提高 GNN 聚合稳定性
- 保持图稀疏性

---

# 9. 语义-行为联合采样（SBJS）

# Semantic-Behavior Joint Sampling

本研究在 FLAG 的 Semantic Sampling 基础上进行扩展。

---

## 9.1 联合采样流程

对于目标用户：

1. 获取 2-hop 邻域；
2. 计算语义相似度；
3. 计算行为相似度；
4. 进行联合评分；
5. 选择 Top-K 邻居；
6. 构建目标子图。

---

## 9.2 联合采样优势

相比单语义采样：

- 能降低伪装文本干扰；
- 提高邻域纯度；
- 强化团伙结构学习；
- 提升 GNN 聚合效果。

---

# 10. 双通道 LLM 特征生成

本研究采用：

# Qwen2.5

替代 FLAG 中的 Gemma。

模型采用 LoRA 微调方式。

---

## 10.1 语义通道

语义通道主要生成：

- 情感特征
- 语言风格
- 文本可读性
- 情绪表达
- 修辞特征

用于增强语义判别能力。

---

## 10.2 行为通道

行为通道主要生成：

- 行为模式
- 时间规律
- 评论异常
- 协同行为风险
- 刷评模式

用于增强行为伪装识别能力。

---

# 11. Frozen LM 编码

双通道生成文本通过 Frozen LM 编码。

采用：

- MiniLM
- Sentence-BERT

编码器参数冻结。

---

## 11.1 参数冻结原因

冻结编码器能够：

- 降低训练成本
- 避免过拟合
- 保持语义空间稳定
- 提高训练稳定性

最终生成：

- 语义向量
- 行为向量

作为 GNN 输入。

---

# 12. DualGNN 图表示学习

采用：

- GCN
- GAT

作为图学习 Backbone。

---

## 12.1 双通道共享参数

语义通道与行为通道：

- 共用同一套 GNN 参数；
- 在同一 HCCG 图上传播。

---

## 12.2 GNN 学习目标

GNN 主要学习：

- 局部协同行为
- 欺诈团伙结构
- 伪装邻域模式
- 用户关联关系

---

# 13. 双通道注意力融合

由于不同用户对语义与行为依赖程度不同，因此采用注意力机制动态融合。

## 13.1 注意力机制作用

注意力模块用于：

- 自适应调整语义权重
- 自适应调整行为权重
- 动态融合双通道表示

最终输出统一节点表示用于分类预测。

---

# 14. 多目标联合优化

系统采用多目标损失函数联合优化。

包括：

| 损失函数 | 作用 |
|---|---|
| 主分类损失 | 欺诈分类 |
| 辅助监督损失 | 双通道独立学习 |
| 正交损失 | 降低通道冗余 |
| 一致性损失 | 保持预测稳定性 |

---

## 14.1 正交损失

用于约束：

- 语义表示
- 行为表示

学习不同信息，避免通道退化。

---

## 14.2 一致性损失

用于约束：

双通道预测结果保持一致。

提升：

- 鲁棒性
- 泛化能力

---

# 15. 交替优化训练策略

训练采用：

# 交替优化

包括两个阶段。

---

## 阶段 A：固定 GNN

训练：

- Qwen2.5（LoRA）

优化双通道生成质量。

---

## 阶段 B：固定 LLM

训练：

- GCN/GAT

优化图结构表示学习。

---

# 16. 推理流程

测试阶段执行流程如下：

1. 输入目标用户；
2. 获取联合采样子图；
3. 双通道文本生成；
4. Frozen LM 编码；
5. 图传播；
6. 注意力融合；
7. 输出欺诈概率。

最终输出：

- 正常用户
- 欺诈用户

预测结果。

---

# 17. 创新点总结

本研究主要创新包括：

## 创新点1：语义-行为双通道建模

突破 FLAG 单语义建模限制。

---

## 创新点2：HCCG 协同伪装图

显式建模：

- 协同行为
- 商品重叠
- 时间一致性

---

## 创新点3：联合邻居采样

同时利用：

- 语义
- 行为

提升邻域质量。

---

## 创新点4：LLM 引导图结构增强

不仅增强节点特征：

还增强图结构。

---

## 创新点5：共享参数 DualGNN

实现双通道协同图学习。

